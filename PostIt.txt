{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": {
          "node": "10"
        }
      }
    ],
    "@babel/preset-react"
  ]
}
entry: ['babel-polyfill', './index.js'],

regenerator-runtime
axios


// import React, { useState, useEffect } from 'react';
// import axios from 'axios';
// const url = 'http://localhost:1337'

// function App() {
//   const [data, setData] = useState([]);
//   useEffect(() => {
//     const fetchData = async () => {
//       const result = await axios(
//         'http://localhost:1337/restaurants/',
//       );
//       setData(result.data);
//     };
//     fetchData();
//   }, []);
//   return (
//     <ul>
//       {data.map(item => (
//         <li key={item.id}>
//           <a >{item.name}</a>
//           <img src={url + item.Image.url} alt=""/>
//         </li>
//       )) }
//     </ul>
//   );
// }
// export default App;

Hola con todos, buenas tardes,
El presente correo es para informarles sobre la situación actual referente a los html; En estos días tanto el equipo de Arrivedo como yo hemos estado intentando poder remover la extension .html de las paginas de rutas cortas de diversas formas sin éxito, ya que no se puede crear un .htaccess ya que no responde xq es un proyecto en .NET y solo hace caso al web.config. Por ende intentamos trabajar con el web.config de la página y tampoco funciono, tuve un call con @Erick Ibarra y todo. 

La solución más factible me parece es convertir los archivos de html en parte del proyecto .NET para que no hayan problemas en la integración y funcione con el web.config, hable con Ronald de promperu y el también me recomendo que es la mejor opción. En la otra mano, @Erick Ibarra me comenta que pasar los html generados por Arrivedo a .aspx e integrarlos al proyecto .NET traería problemas con la programación de los mapas. 

Dicho esto quisiera saber que accion se puede tomar y llegan a un acuerdo en que se debería hacer con las áaginas para poder así dar una solución final, ya que el cliente anda preocupado por estos temas.

Gracias de antemano, espero su respuesta.


Para: Alex Diaz <alex.diaz@havasmedia.com>; Andre Cruz <andre.cruz@havas.com>
CC: Alonso Franco <alonso@arrivedo.com>; Erick Ibarra <erick@kipulab.com>; Rodrigo Lopez <rodrigo@arrivedo.com>; Jaime Solís <jaime@kipulab.com>
    
    
    // PARA CREAR UN ARRAY CON LOS OBJETOS QUE CUMPLAN LA CONDICION
    // const filteredItems = products.map((item) => item.name.length)

    // PARA ENCONTRAR OBJETOS QUE CUMPLAN AL CONDICION, RETORNA VARIOS
    // const filteredItems = products.filter((item) => item.price <= 100)

    // PARA ENCONTRAR UN OBJETO EN ESPECIFICO, SOLO RETORNA EL PRIMERO
    const products = [
      { name: `Bike`, price: `200` },
      { name: `TV`, price: `200` },
      { name: `Album`, price: `10` },
      { name: `Book`, price: `5` },
      { name: `Phone`, price: `500` },
      { name: `Computer`, price: `1000` }
    ]

    const filteredItems = products.find(item => item.price <= 100)
    console.log(filteredItems)

www.thenekodark.com
www.mazterize.com


 